import { Dialog, DialogContent, DialogHeader } from "./ui/dialog";

export default function CreateChannelModel(){
    return(
        <Dialog>
            <DialogContent>
                <DialogHeader>Create a Channel</DialogHeader>
            </DialogContent>
        </Dialog>
    )
}